name: Deploy React Project to Plesk Server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '18'

    - name: Install dependencies
      run: |
        cd proje adı
        npm install
        npm run build

    - name: Upload build to Plesk Server
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: 94.73.147.91
        username: u1876828
        password: AnaninAmiGalatasaray1
        local-dir: ./project-name/build/
        server-dir: /latifaltay.com/

    - name: Clean up the build folder
      run: rm -rf ./project-name/dist/
